# Reproducibility environment for the Color-Reliance Diagnostic.
# Captured from the colorreliance conda env (Python 3.11.15) used for every run.
# Full pip freeze per run is archived under logs/<run>_env/pip_freeze.txt.
#
# IMPORTANT (GPU): torch/torchvision are the CUDA 12.8 builds for an RTX 5080
# (Blackwell, sm_120). Install them from the PyTorch cu128 index, e.g.:
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
# then install the rest with: pip install -r requirements.txt

torch==2.11.0          # +cu128 build (see note above)
torchvision==0.26.0    # +cu128 build (see note above)
timm==1.0.27
numpy==2.4.4
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
matplotlib==3.11.0
pillow==12.2.0
PyYAML==6.0.3
thop==0.1.1.post2209072238   # params / FLOPs counting (Table II)
tqdm==4.68.3
tabulate==0.10.0       # DataFrame.to_markdown for table export
rich==15.0.0
typer==0.25.1
